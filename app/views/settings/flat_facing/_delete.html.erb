<%= render_alert_dialog do %>
  <%= alert_dialog_trigger do %>
   <span class="inline-flex items-center  bg-rose-500 hover:bg-rose-600 text-white  hover:text-white text-xs py-1 px-2 rounded-md cursor-pointer"><%= render 'icons/delete' %></span> 	
    <% end %>

  <%= alert_dialog_content do %>
  <div class="flex flex-col space-y-2 text-center sm:text-left whitespace-normal">
    <h2 class="text-lg font-semibold">Are you absolutely sure?</h2>
    <p class="text-sm text-muted-foreground">
      This action cannot be undone. This will permanently delete your account and remove your data from our servers.
    </p>
  </div>
  <% end %>

  <%= alert_dialog_cancel do %>
    <%= render_button("Cancel", variant: :outline) %>
  <% end %>

  <%= alert_dialog_continue do %>
    <%= render_button("Continue", class: "bg-rose-500 hover:bg-rose-600") %>
  <% end %>
<% end %>